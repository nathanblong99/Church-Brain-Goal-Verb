######################################################################
# Church Ops Agent Environment Configuration
# Copy this file to `.env` and fill in the secrets/values you have.
# Any variable left unset uses the documented default behavior.
######################################################################

###############################
# 1. Core LLM / Planning
###############################
# GEMINI_API_KEY (required for live LLM calls)
# Obtain from Google AI Studio. If omitted:
#  - Planner tests skip
#  - Inbound classifier returns { intent: 'unknown' } for freeform messages
#  - Reply generator falls back to canned phrases
GEMINI_API_KEY=

# GEMINI_MODEL (optional) Default: gemini-1.5-pro
# Alternatives: gemini-1.5-flash, gemini-1.5-flash-8b (cheaper/faster, less reasoning)
GEMINI_MODEL=gemini-1.5-pro

# PLANNER_MAX_TOKENS (optional) Default: 2000
# Upper bound on LLM output tokens for planning / classification / replies.
PLANNER_MAX_TOKENS=2000

###############################
# 2. (Future) SMS Provider (Twilio placeholder)
###############################
# These are NOT currently consumed in code, but reserved for wiring an SMS adapter.
# Leave blank until integration is implemented.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_MESSAGING_SERVICE_SID=
# Enable flag (string boolean). Expect future adapter to check this.
TWILIO_ENABLED=false

###############################
# 3. Optional Runtime Flags
###############################
# NODE_ENV: development | test | production (affects logging/optimizations if added)
NODE_ENV=development
## Potential future multi-tenant hints (currently unused):
# DEFAULT_TENANT_ID=demo
# DEFAULT_CAMPUS=main

###############################
# 4. Testing Guidance
###############################
# For deterministic CI, omit GEMINI_API_KEY so network calls are skipped.
# For local exploratory runs, set the key and re-run `npm test` to exercise live planning.
# Use a smaller GEMINI_MODEL (e.g. gemini-1.5-flash) to lower latency cost during dev.

###############################
# 5. Security Notes
###############################
# NEVER commit your real `.env`. `.gitignore` already excludes it.
# Rotate your GEMINI_API_KEY if accidentally exposed.

###############################
# End of file
###############################
